'use strict';

var totalPrice = prompt('¿Cuánto ha sido la cuenta?');
var diners = prompt('¿Cuánto ha sido la cuenta?');

var pricePerDiner = parseInt(totalPrice) / parseInt(diners);

alert('Cada comensal debe pagar: ' + pricePerDiner);
